// By convention, we expect a class config to call this file at the end,
// potentially having defined +class_alt and -class_alt. They're fine left
// undefined; see ../game_overrides.cfg.

bind ALT +class_alt
-class_alt

echo "Class postscript applied."